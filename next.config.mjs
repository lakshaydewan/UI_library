/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'plus.unsplash.com'], // This should work for most cases but is not recommended for production
    },
  };
  
export default nextConfig;
